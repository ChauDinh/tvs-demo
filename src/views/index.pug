head
  title TVS Pharmacy
  script(src="https://kit.fontawesome.com/f358d85d2a.js" crossorigin="anonymous")
body
  h1 TVS Pharmacy
  
  if users.length === 0 
    h3 Users list
    button 
      a(href="/account/register") Register
  else 
    h3 Users list
    button 
      a(href="/account/register") Register
    ul
      each user in users 
        li= user.username
